---
titulo: Handoff — sessao 54f1c621 (bootstrap ate DSGN-029)
repo: RVM.DesignSystem
data: 2026-09-09
sessao: 54f1c621-e3ca-48f1-a114-0d2ec111507b
periodo: 2026-09-07 22:43 a 2026-09-09 02:42 (UTC, como no transcript)
---

# Handoff — 09/09/2026

Escrito **retroativamente** na sessao seguinte. A sessao `54f1c621` rodou de 07/09 22:43 a
09/09 02:42 (29 MB de transcript, 62 mensagens do Rafael, ao menos um auto-compact) e cobriu
**do bootstrap `DSGN-001` ate o `DSGN-029`** — as quatro ondas, a `1.0.0` e as tasks de paleta,
aparencia e dashboard. Ela terminou **sem escrever handoff**; este documento cobre o buraco.

## TL;DR — onde retomar

Branch **`dev`**, working tree limpo, `dev` == `origin/master` (nada por promover). **Nao ha
codigo pela metade.** O E2E que estava vermelho desde 08/09 19:18 **foi consertado e promovido no
mesmo dia** (`DSGN-030` e `DSGN-031`, ver item 1): producao verificada com navegador `en-US`
servindo `R$ 3.348,65` e o calendario em portugues. Tudo o mais espera decisao do Rafael.

## Trabalho em andamento

**Nenhum.** Todas as tasks abertas na sessao foram fechadas e promovidas:

| Task | Estado | Onde |
|---|---|---|
| `DSGN-021` cores de estado + gerador de CSS | ✅ producao | tag `v1.1.0` |
| `DSGN-023` paleta repinta o site inteiro | ✅ producao | — |
| `DSGN-024` paleta guardada no `localStorage` | ✅ producao | — |
| `DSGN-025`/`026` preview de aparencia + 164 icones | ⏳ **no ar, TEMPORARIO** | espera escolha |
| `DSGN-027` aparencia Admin (AdminLTE) | ⛔ **reprovada e revertida** | `DSGN-028` |
| `DSGN-028` remocao do Admin + Dashboard refeito | ✅ producao | — |
| `DSGN-029` cores do Bootstrap como tema de exemplo | ✅ producao | `/fundamentos/bootstrap` |
| `DSGN-022` graficos | 🟡 **card aberto, zero codigo** | espera decisao |
| `DSGN-018` timeout do push no BaGet | 🟡 **reaberto pela medicao** | ver abaixo |

Git: `origin/master` = `1a3423a` (PR #57, promocao do `DSGN-029` as 02:40 de 09/09, Deploy Pages
verde). `git status --porcelain` vazio, `git stash list` vazio, nenhuma branch local nao-pushada.

## Proximos passos concretos

### 1. ✅ E2E vermelho — CONSERTADO e EM PRODUCAO (09/09, `DSGN-030` + `DSGN-031`)

`SiteSmokeTests.A_listagem_filtra_ordena_e_pagina_SO_POR_TECLADO` falha com
`System.FormatException : The input string 'BRL96.90' was not in a correct format`, em
`test/playwright/RVM.DesignSystem.E2E/SiteSmokeTests.cs:416` (`LerColunaDeTotais`).

- **Desde quando:** 08/09 19:18, o primeiro run depois que a onda 4 (`DSGN-020`, commit
  `5dda16d`) levou o teste e a tela de Listagem para `dev`. Verde as 14:55 porque o teste ainda
  nao existia. **Ou seja: ele nunca passou contra o site publicado** — o CI de PR nao roda E2E.
- **O que esta acontecendo:** o E2E aponta para `https://design.dev.rvmtech.com.br`
  (`e2e.yml:57`). No runner o Chromium e `en-US`; o Blazor WASM so embarca o shard de ICU da
  cultura do navegador, entao `CultureInfo.GetCultureInfo("pt-BR")` devolve uma cultura **sem
  dados** e `value.ToString("C2", PtBr)` sai `BRL96.90` em vez de `R$ 96,90`.
- **Verificado nos dois lados** (09/09, por Playwright): em `design.rvmit.com.br/padroes/listagem`
  com navegador `pt-BR` a coluna sai correta — `R$ 3.348,65`, `R$ 10.511,61`. O defeito **so
  aparece com navegador em outra lingua**, e por isso passou despercebido.
- **Por que importa alem do CI:** e o `BrlFormatter`. A regra do `CLAUDE.md § Convencoes`
  ("moeda por `BrlFormatter`, nunca interpolacao manual") assume que a cultura explicita basta.
  No WASM ela **nao** basta, e falha em silencio. Um consumidor da biblioteca herda isso.
- **Conserto aplicado** (`DSGN-030`, PR #59, em `dev` as 11:40 de 09/09):
  `BlazorWebAssemblyLoadAllGlobalizationData` no site, `Locale = "en-US"` fixo no teste, e
  **nada na biblioteca** — o `BrlFormatter` estava certo, faltava o dado. Medido: o publish
  encolheu 1,8 MB e o download cresceu ~180 KB brotli. E2E do pipeline verde as 11:41.
  Promovido para `master` as 12:08 de 09/09, com o sinal verde dele.
- **Fica para o consumidor**: um app em WASM que use a biblioteca precisa ligar a mesma flag.
  Registrado no `CLAUDE.md § Convencoes`.
- **O defeito nao parava na moeda** (`DSGN-031`): o calendario do `RvmDatePicker` saia inteiro em
  ingles com navegador `en-US` — confirmado na producao. A mesma flag conserta; entrou o teste
  que prova isso, e ele **falha** contra a producao ate a promocao. ⏳ Fica aberta a pergunta de
  arquitetura: montar `NumberFormatInfo`/`DateTimeFormatInfo` proprios e nao exigir flag nenhuma
  do consumidor.
- ✅ **"Março De 2026" corrigido** (`DSGN-032`, 09/09): o `ToTitleCase` subia cada palavra, que e
  regra de ingles. ⚠️ Este e o unico dos tres que **mexe na biblioteca** — entra como patch na
  proxima publicacao do pacote, e a `v1.1.0` ainda nem esta no feed (item 2).

### 2. ✅ RESOLVIDO em 09/09 (`DSGN-033`) — a `1.1.1` esta no feed

Feito o que o `DSGN-018` propunha: `timeout 180` do coreutils **por fora** da tentativa, com o
`--timeout 120` mantido — um corta a requisicao, o outro corta a tentativa inteira. Publicadas a
`1.1.2-alpha.23` (em `dev`) e a estavel **`1.1.1`** (tag `v1.1.1`, commit `7e6cc03`), as duas em
runs de 27 segundos.

⚠️ **A `1.1.0` continua com tag e sem pacote, de proposito**: aquela tag prende um commit
anterior ao `DSGN-032`, e reusar o numero com codigo novo seria o alvo movel que o `DSGN-009`
proibiu. O feed pula de `1.0.0` para `1.1.1`.

⏳ **Sobra disto**: o `UseLocalDesignSystem` continua `true` — o site compila por
`ProjectReference`, nunca pelo pacote. Agora que existe estavel no feed, virar para `false`
passou a ser possivel; e mudanca de build do site, e nao foi feita.

### 3. 🟡 `DSGN-022` — graficos, decisao do Rafael

Card aberto de proposito sem codigo: "embrulhar biblioteca existente" (roadmap), "sem dependencia
NuGet de terceiros" e "nada depende de JS para renderizar" (`CLAUDE.md`) nao cabem juntos.
Quatro saidas descritas no card (**A** SVG gerado em C# — recomendada; **B** biblioteca JS;
**C** pacote .NET de terceiro; **D** ficar de fora).

### 4. ⏳ Preview de aparencia — sai inteiro quando ele escolher

Seletor "Aparencia" na topbar com `Atual`, `Sobrio`, `Marcante` e `Vivo`. A lista do que remover
esta no `CLAUDE.md` e vale por inteiro: `wwwroot/css/aparencia.css`, `wwwroot/js/aparencia.js`,
`Documentacao/AparenciaViva.cs`, o seletor no `MainLayout` e o teste
`As_duas_aparencias_do_preview_passam_no_axe`. Nada disso e a biblioteca.

### 5. ⏳ `scrollable-region-focusable` no `RvmAppShell`

Tela sem nenhum elemento focavel reprova no axe, porque a area de conteudo do shell rola e nao ha
como rola-la pelo teclado. Pegou o Dashboard e foi contornado **na tela** (cada caixa virou link).
A resposta certa seria no `RvmAppShell`, e nao foi feita — um app consumidor com tela so de
leitura herda o problema.

## Decisoes tomadas na sessao (as que o git nao conta)

- **Regra de icones revista** (`DSGN-026`, a pedido dele): de 27 para **164**, curados **por
  aplicacao** e nao por componente. A regra da onda 2 otimizava a coisa errada — quem consome
  monta telas, nao componentes. E o argumento de custo que sustentava a regra antiga **caiu na
  medicao**: ~300 bytes por icone-peso. O motivo real de curar e superficie de API, nao byte.
- **Tingir superficie e motor de tema, nunca folha de estilo** — um cinza escrito a mao fura o
  portao de contraste em silencio. Por isso a aparencia `Vivo` e C# (`AparenciaViva.cs`) e mexe
  muito na croma e pouco na luminosidade: o `EnsureContrast()` re-deriva os papeis coloridos,
  mas **nao** o `on-surface`.
- **A paleta guardada e a das cores escolhidas, nunca o tema derivado** (`DSGN-024`) — assim uma
  paleta antiga passa pelas regras de contraste de hoje, em vez de virar copia congelada que
  nenhuma correcao do `FromSeed` alcanca.
- **Tema de terceiro vive no site, nao no pacote** (`DSGN-029`, `Documentacao/TemasDeExemplo.cs`):
  paleta do Bootstrap nao e identidade nossa e nome publico e compromisso. Achado do card:
  **nenhuma das seis cores do Bootstrap atinge 4,5:1** contra superficie clara — quatro erram por
  uma casa decimal, `info` e `warning` erram muito e o amarelo `#FFC107` vira ambar `#955B00`.
  Nao e defeito deles: as cores do Bootstrap sao pensadas como **fundo**.
- **A imitacao do AdminLTE foi reprovada** por ele (`DSGN-027` → `DSGN-028`). Ficou a tela
  `/padroes/dashboard` na linguagem daqui. Nao recriar sem pedido; a tecnica da casca escura
  esta descrita no `DSGN-027`.
- **Versao estavel so por TAG** (`DSGN-009`, anterior a esta sessao, mas e o que sustenta o
  item 2): push em `master` nao publica.

## Estado do ambiente

- **Prod:** `https://design.rvmit.com.br` (GitHub Pages, branch `master`) — no ar com o
  `DSGN-029`; `/fundamentos/bootstrap` confirmada viva em 09/09.
- **Dev:** `https://design.dev.rvmtech.com.br` (Rivendell, rsync, branch `dev`).
- ⚠️ **Rota profunda responde HTTP 404 com a pagina certa** — confirmado de novo hoje em
  `/padroes/listagem`. E o fallback do Pages servindo `404.html`; verificacao de deploy e por
  **conteudo**, nunca por codigo HTTP. E por isso que o monitor do Kuma (`DesignSystem - prod`)
  e do tipo `keyword`.
- Sem banco, sem container, sem `/health`, sem dado criado. Nada a rotacionar.

## Pendencias do Rafael

1. **Aparencia**: escolher entre `Atual`, `Sobrio`, `Marcante` e `Vivo` — o preview inteiro sai
   depois disso.
2. **`DSGN-022` graficos**: qual das quatro saidas.
3. **Decidir a pergunta de arquitetura do `DSGN-031`**: a biblioteca deveria montar
   `NumberFormatInfo`/`DateTimeFormatInfo` proprios e parar de exigir a flag de todo consumidor
   WASM? Hoje a exigencia e so documentacao — e quem esqueceu a flag da primeira vez foi este
   proprio repositorio.
4. **Adocao**: nao ha onda 5. O proximo projeto RVM que precisar de UI e quem prova a biblioteca.

## Detalhe solto encontrado ao escrever este handoff

O `CLAUDE.md` cita `Vault/05_References/baget-sem-backup.md` e **o arquivo nao existe** nessa
pasta (so os dois ADRs). Nao investiguei; pode ter ficado so no texto do `DSGN-009`.

## A segunda metade do dia (09/09, tarde)

Depois do handoff acima, a sessao seguiu e o dia rendeu mais tres blocos:

1. **`DSGN-032`** — "Março De 2026" virou "Março de 2026". `ToTitleCase` e regra de ingles.
2. **`DSGN-033`** — `timeout 180` por fora do push do BaGet, e a **`1.1.1` publicada** no feed.
   A `1.1.0` continua com tag e sem pacote, de proposito.
3. **`PRODUCT.md` + `DESIGN.md`** na raiz, e a primeira rodada do `impeccable critique` — que
   rendeu **sete cards** (`DSGN-034` a `DSGN-040`), todos implementados e em producao no
   mesmo dia.

⚠️ **O achado que vale guardar** esta no `DSGN-038`: o portao de contraste media
`border-strong` **so contra `surface`**. Acrescentados os pares que faltavam, 6 das 12 paletas
reprovaram — violacao de 1.4.11 que estava em producao em todo tema escuro. A licao repete a do
`DSGN-018`: **um par que existe na tela e nao existe na lista nao e medido por ninguem.**

⚠️ **Armadilha de infra vista hoje**: o E2E falhou tres vezes seguidas com `Failed to install
browsers` (o `apt` do runner do GitHub sem baixar indices, exit 100). **Nao e o codigo, e nenhum
teste chega a rodar** — meia hora depois voltou sozinho. Antes de investigar codigo, olhe se a
falha esta no passo "Instala browsers do Playwright".

## Comandos uteis

```bash
cd /c/IA/RVM.DesignSystem
git checkout dev && git pull

# CI e E2E
gh run list --limit 10
gh run list --workflow=e2e.yml --limit 5

# E2E a mao (o workflow so dispara do branch padrao)
E2E_BASE_URL=https://design.dev.rvmtech.com.br \
  dotnet test test/playwright/RVM.DesignSystem.E2E

# build (cobertura local NAO roda nesta maquina — ver memoria do projeto)
dotnet build RVM.DesignSystem.slnx -c Release
```
