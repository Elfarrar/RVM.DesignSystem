namespace RVM.DesignSystem.Components.Breadcrumbs;

/// <summary>O que separa os passos — as duas colunas da pagina Breadcrumbs do kit.</summary>
public enum RvmBreadcrumbSeparator
{
    /// <summary>Barra "/" (coluna "Text").</summary>
    Slash,

    /// <summary>Seta para a direita (coluna "Icon").</summary>
    Chevron
}
