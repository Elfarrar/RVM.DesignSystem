namespace RVM.DesignSystem.Components.Pagination;

/// <summary>Estilo dos itens — as duas metades da pagina Pagination do kit.</summary>
public enum RvmPaginationVariant
{
    /// <summary>Sem borda; so a pagina atual ganha fundo.</summary>
    Text,

    /// <summary>Todo item com borda.</summary>
    Outlined
}
