namespace RVM.DesignSystem.Components.Alert;

/// <summary>Os tres estilos da pagina Alert do kit.</summary>
public enum RvmAlertVariant
{
    /// <summary>Fundo suave com texto na cor do papel — o padrao, o menos estridente.</summary>
    Standard,

    /// <summary>Preenchido com a cor do papel. Para o que nao pode passar despercebido.</summary>
    Filled,

    /// <summary>So contorno.</summary>
    Outlined
}
