@namespace RVM.DesignSystem.Components.Radio
@typeparam TValue
@using Microsoft.AspNetCore.Components.Forms

@* Uma opcao do RvmRadioGroup. O input e o <InputRadio> do Blazor, que so funciona dentro de um
   InputRadioGroup — por isso o RvmRadio precisa estar dentro de um RvmRadioGroup. *@
<label class="@ClassesDaRaiz">
    <span class="rvm-alvo">
        <InputRadio TValue="TValue" Value="Value" class="rvm-nativo" @attributes="AdditionalAttributes" />
        <span class="rvm-circulo" aria-hidden="true">
            <span class="rvm-ponto"></span>
        </span>
    </span>

    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <span class="rvm-rotulo">@Label</span>
    }
    else
    {
        @ChildContent
    }
</label>
