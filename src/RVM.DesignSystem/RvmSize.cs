namespace RVM.DesignSystem;

/// <summary>
/// Tamanho de componente interativo. Botao, campo, chip e avatar usam os tres.
/// </summary>
public enum RvmSize
{
    /// <summary>Compacto — tabela, barra de ferramentas.</summary>
    Small,

    /// <summary>Padrao.</summary>
    Medium,

    /// <summary>Destaque — acao principal de uma tela.</summary>
    Large
}
